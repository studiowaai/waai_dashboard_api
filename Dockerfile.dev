FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package*.json ./
RUN npm ci

# Copy source + config
COPY tsconfig.json nest-cli.json ./
COPY src ./src

EXPOSE 3000

# NestJS watch mode
CMD ["npm", "run", "start:dev"]
