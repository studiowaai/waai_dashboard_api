# Dashboard API Environment Variables

# Database connection string (required)
# Format: postgresql+asyncpg://username:password@host:port/database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/waai_db

# CORS allowed origins (required)
# Comma-separated list of frontend URLs, or "*" for all origins
# Examples:
#   Single origin: http://localhost:3000
#   Multiple origins: http://localhost:3000,https://dashboard.yourdomain.com
#   All origins: *
CORS_ORIGIN=http://localhost:3000

# JWT Secret Key (required)
# IMPORTANT: Change this to a long random string in production!
# Generate with: openssl rand -hex 32
JWT_SECRET=change-me-to-a-secure-random-string-at-least-32-chars

# JWT Token Expiration (optional)
# Time in minutes before JWT tokens expire
# Default: 43200 (30 days)
# Examples: 1440 (1 day), 10080 (7 days), 43200 (30 days)
JWT_EXPIRE_MIN=43200
